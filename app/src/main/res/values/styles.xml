<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Cores do tema -->
        <item name="colorPrimary">@color/CorPrimaria3</item>
        <item name="colorPrimaryDark">@color/CorPrimariaEscura3</item>
        <item name="colorAccent">@color/AtenuacaoDeCor3</item>
        <!-- Cor do TextInputLayout -->
        <item name="colorControlActivated">@color/CorLogoForte</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Style para iniciar o logo em fullscreen, melhorando o design do aplicativo.-->
    <style name="AppCompat.TelaCheia" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
